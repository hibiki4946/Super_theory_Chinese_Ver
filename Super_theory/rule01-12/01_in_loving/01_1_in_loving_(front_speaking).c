[多愛_前言]
領域單元 : 構成領域內容的單元 像是 ㄅ 為 該語言的最小單元 可用外部匯入進入表層記憶
表層記憶 : 正在處理該領域單元 可接受中層匯入 或 深層匯入 或 外部匯入
這層的領域單元未在處理時 一段時間後 進到中層記憶
中層記憶 : 未在處理該領域單元 需要時可提取到表層記憶處理 提取後會建立強連接(下次提取速度上升)
提取並處理完成 該領域單位會在一段時間後 進到中層記憶
深層記憶 : 未在處理該領域單元 需要時通常無法提取到表層記憶處理 但確定該領域單元現存於記憶
提取後會建立強連接(下次提取速度上升)
提取並處理完成 該領域單位會在一段時間後 進到中層記憶
鎖定記憶 : 未在處理該領域單元 通過特殊方法 或 由深層記憶自然到鎖定記憶 無法確認該領域單元現存於記憶
